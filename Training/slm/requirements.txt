# Fine-tuning Qwen3.5-0.8B for AI-PEER
# Install: pip install -r requirements.txt
# Requires: CUDA-capable GPU with >= 8GB VRAM

unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
torch>=2.4.0
datasets>=3.0.0
trl>=0.12.0
transformers>=4.46.0
google-cloud-storage>=2.18.0
